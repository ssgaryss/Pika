#include "pkpch.h"
#include "Math.h"

namespace Pika::Math{

    // TODO!
    bool Pika::Math::decomposeTransform(const glm::mat4& vTransform, glm::vec3& vTranslation, glm::vec3& vRotation, glm::vec3& vScale)
    {
        return false;
    }

}
